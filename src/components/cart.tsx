import { Button } from "./ui/button"
import { Sheet, SheetTrigger } from "./ui/sheet"


const Cart = () => {
    return(
        <Sheet>
            <SheetTrigger>
                A
            </SheetTrigger>
        </Sheet>
    )
}

export default Cart